spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5435/postgres
    username: postgres
    password: password
  jpa:
    open-in-view: false
    show-sql: true
server:
  port: 8084